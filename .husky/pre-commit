# Vérifier s'il y a des fichiers stagés
if git diff --cached --quiet; then
  echo "Aucun fichier stagé, skip versioning"
  exit 0
fi

# Récupérer la dernière version taggée
LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null)

# Si aucun tag n'existe, c'est la première release
if [ -z "$LAST_TAG" ]; then
  echo "🆕 Première release détectée"
else
  # Vérifier s'il y a des commits depuis le dernier tag
  COMMITS_SINCE=$(git rev-list ${LAST_TAG}..HEAD --count 2>/dev/null)
  if [ "$COMMITS_SINCE" -eq "0" ]; then
    echo "Aucun nouveau commit depuis ${LAST_TAG}, skip versioning"
    exit 0
  fi
fi

# Exécuter standard-version pour bumper la version
echo "🔄 Versionnage automatique en cours..."
npm run release -- --skip.commit --skip.tag

# Ajouter les fichiers modifiés par standard-version au commit
git add package.json package-lock.json CHANGELOG.md

echo "✅ Version mise à jour automatiquement"
